{
  "name": "browser-use-cursor",
  "private": true,
  "version": "0.1.0",
  "description": "Cursor plugin and browser extensions so agents can drive your real Chrome, Edge, Brave, Firefox, and other extension browsers.",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --if-present build && node scripts/generate-icons.mjs && node scripts/package-plugin.mjs",
    "sync": "pnpm build && node scripts/sync-local-plugin.mjs",
    "install-hosts": "node scripts/install-native-hosts.mjs",
    "easy-install": "pnpm sync && node scripts/easy-install.mjs",
    "package-store": "pnpm build && node scripts/package-store.mjs",
    "smoke": "pnpm build && node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2",
    "esbuild": "^0.25.9",
    "playwright": "^1.55.0",
    "typescript": "^5.9.2"
  }
}
